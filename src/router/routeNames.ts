export enum ROUTE_NAMES {
  HOME = "/",
  LOGIN = "/login",
  USERS = "/users",
  STATISTICS = "/statistics",
  COOKBOOKS = "/cookbooks",
  RECIPES = "/recipes",
  COLLECTIONS = "/collections",
  SETTINGS = "/settings",
}
