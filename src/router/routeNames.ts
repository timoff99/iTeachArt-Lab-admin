export enum ROUTE_NAMES {
  HOME = "/",
  LOGIN = "/login",
  USERS = "/users",
  STATISTICS = "/statistics",
  COOKBOOKS = "/cookbooks",
  COOKBOOKSID = "/cookbooks/:id",
  RECIPES = "/recipes",
  COLLECTIONS = "/collections",
  SETTINGS = "/settings",
}
